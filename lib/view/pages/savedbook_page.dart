import 'package:flutter/material.dart';

class SavedbookPage extends StatelessWidget {
  const SavedbookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Saved Books')));
  }
}
